Example:
  handler: src/main/support/serverless/Dispatcher.Example
  description: "Example"
  ephemeralStorageSize: 512
  events:
    - http:
        authorizer: ${self:custom.roleAuthorizer}
        cors:
          allowCredentials: true
          headers:
            - Access-Control-Allow-Origin
            - Content-Type
          origin: ${self:custom.environment.COHOR_APPLICATION_FRONTEND_ORIGIN.${self:custom.stage}}
        method: "GET"
        path: "/protected"
  maximumRetryAttempts: 0
  memorySize: 512
  timeout: 30
